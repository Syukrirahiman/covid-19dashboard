const white = "#FFFFFF";
const themeColor = "#003159";

const palette = {
    primary: {
        contrastText: white,
        dark: themeColor,
        main: themeColor,
        light: themeColor
    }
    ,
    theme: {
        contrastText: white,
        dark: themeColor,
        main: themeColor,
        light: themeColor
    }
}

export default palette;